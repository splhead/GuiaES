<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- <CheckBoxPreference
        android:key="example_checkbox"
        android:title="@string/pref_title_social_recommendations"
        android:summary="@string/pref_description_social_recommendations"
        android:defaultValue="true" /> -->

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <PreferenceCategory android:title="@string/pref_conta">
        <EditTextPreference
            android:key="email"
            android:title="@string/pref_email"
            android:hint="@string/pref_email_exemplo"
            android:selectAllOnFocus="true"
            android:inputType="textEmailAddress"
            android:capitalize="words"
            android:singleLine="true"
            android:maxLines="1" />

        <EditTextPreference
            android:key="senha"
            android:title="@string/pref_senha"
            android:hint="@string/pref_senha_exemplo"
            android:selectAllOnFocus="true"
            android:inputType="textPassword"
            android:password="true"/>

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <!-- <ListPreference
        android:key="example_list"
        android:title="@string/pref_title_add_friends_to_messages"
        android:defaultValue="-1"
        android:entries="@array/pref_example_list_titles"
        android:entryValues="@array/pref_example_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" /> -->
    </PreferenceCategory>
    <PreferenceCategory android:title="@string/pref_tipo">
        <ListPreference
            android:title="Meditação"
            android:summary="Qual tipo mostrar"
            android:key="tipo"
            android:entries="@array/pref_tipo_list"
            android:entryValues="@array/pref_tipo_list_values" />
    </PreferenceCategory>
</PreferenceScreen>
